[Bank]
; Optional panel size percentage, 100 to 150. Omit to follow the game's DPI context.
; UiScale=125
; The panel reuses the authenticated game session on these existing ports.
CharacterPort=6121
MapPort=5121
; Opt-in local latest-state report, without account IDs, tokens or balances.
Diagnostics=0
